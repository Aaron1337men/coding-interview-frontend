class SnackbarConstants {
  static const fadeInDuration = Duration(milliseconds: 200);
  // Time is 2 plus 400 milliseconds of fade in and fade out plus 600 milliseconds for free
  static const snackbarDuration = Duration(milliseconds: 3000);
  static const snackbarWithoutLasAnimDuration = Duration(milliseconds: 2000);
}
